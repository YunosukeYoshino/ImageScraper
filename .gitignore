# ---- Python build / virtual env ----
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Virtual environments
.venv/
.env/
venv/

# uv lock / cache (keep lock file, ignore caches)
.cache/
.ruff_cache/

# Test output / temporary dirs
.tmp_test_out/

# Local image download directories (regenerated by scraper)
images/
downloaded_hanako/

# Credentials / secrets (place under `credentials/` or `secrets/` locally)
credentials/
secrets/
*service*account*.json
*_credentials.json
*-sa.json
*.pem
*.key
*.crt

# OS / editor noise
.DS_Store
.idea/

# Logs
*.log

# Optional: coverage / reports
htmlcov/
.coverage*

# Node / front-end (future-proofing if added later)
node_modules/

# Backup / artifacts
*.bak
*.swp

# Ignore uv build artifacts (if created)
build/

discovery_logs/

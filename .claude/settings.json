{
  "permissions": {
    "defaultMode": "bypassPermissions"
  },
  "enableAllProjectMcpServers": true,
  "model": "opus",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r 'if .tool_input.command | test(\"bun run dev\") then {\"decision\": \"block\", \"reason\": \"not allowed\"} else empty end'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_FROM | select(endswith(\".ts\") or endswith(\".tsx\"))' | xargs -r bun run check"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_FROM | select(endswith(\".ts\") or endswith(\".tsx\"))' | xargs -r bun run format"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_FROM | select(endswith(\".ts\") or endswith(\".tsx\"))' | xargs -r bun run test"
          }
        ]
      }
    ]
  }
}
